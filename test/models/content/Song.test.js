'use strict'

const t = require('tap')
const Song = require('../../../server/models/content/Song') // eslint-disable-line no-unused-vars

t.todo('gets song from requested genre')
t.todo('does not return an excluded song')
